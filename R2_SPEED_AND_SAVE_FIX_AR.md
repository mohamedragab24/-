# إصلاح السرعة والحفظ — V15

تم تعديل المنصة في هذه النسخة:
- رقم الدرس يظهر إجباريًا بالشكل `1- عنوان الدرس` ثم `2- عنوان الدرس` بدون النقطة.
- الرقم يعاد ترتيبه تلقائيًا عند الحذف وإعادة الترتيب والحفظ.
- الكورس الجديد يُحفظ كـ `pending` وغير منشور حتى تتم مراجعته.
- الحفظ يستخدم UID الحقيقي للحساب المسجل في Firebase بدل قيمة قديمة أو تجريبية.
- عند فشل Firebase يظهر سبب الخطأ الحقيقي بدل رسالة عامة فقط.
- Worker يدعم HTTP Range للفيديو، وهذا مهم جدًا لتشغيل MP4 الكبير بالتحميل الجزئي والـseek بدل سحب الملف كاملًا.
- Worker يرسل `Accept-Ranges` و`Content-Range` و`Content-Length` وCache-Control.

## مهم بعد رفع النسخة
يجب نشر ملف `cloudflare-worker/worker.js` الجديد على نفس Worker:
`fahmny-r2`

لأن إصلاح سرعة الفيديو لن يظهر إذا ظل الـWorker القديم منشورًا.

لا يوجد Flutter SDK في بيئة إعداد الملف، لذلك لم يتم ادعاء عمل build للتطبيق هنا.
