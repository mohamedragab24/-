# تحديث مركز الاعتماد الموحد V17

- تمت إضافة تبويب «مراجعة الكورسات» داخل «مركز الاعتماد والرقابة الموحد».
- الكورسات بحالة `pending` تظهر للمسؤول للمراجعة.
- القبول يحولها إلى `published`، والرفض يتطلب سببًا ويُحفظ في `courses/{courseId}/reviews`.
- تطبيق Flutter يحتوي على مراجعة الكورسات داخل «مركز الاعتماد الموحد»، مع قبول/رفض وسجل المراجعات.
- صلاحية التطبيق تعتمد على `users/{uid}.isAdmin == true` أيضًا، وليس `role == admin` فقط.
